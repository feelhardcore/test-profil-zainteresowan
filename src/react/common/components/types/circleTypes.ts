import { Colors, Events } from "../../../types/types.ts"

export interface CircleProps{
    backgroundColor? : Colors
    dimensions? : number
    width? : number
    events? : Events
    outlineColor? : Colors

}